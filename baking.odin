package main

bake_rooms :: proc(world: ^World, state: ^MapScreenState) {
	if !state.dirty do return

	//For earch room, draw it's texture with the correct rotation at the corresponding position


	
	state.dirty = false
}
